using System.Collections;
using System.Collections.Generic;
using TMPro;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.EventSystems;
using UnityEngine.UI;

public class Slot : MonoBehaviour
{
    public GameObject placeHolderPrefab;
    GameObject placeHolder;
    private void Start()
    {
        //placeHolder = Instantiate(placeHolderPrefab, transform);
        //if (gameObject.GetComponentInChildren<ActionCardDisplay>())
        //{
        //    gameObject.GetComponent<ActionCardDisplay>().GetCardInfo(gameObject.GetComponent<ActionCardDisplay>().actionCardData);
        //}
    }
}
